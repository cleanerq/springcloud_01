server:
  port: 8006
spring:
  application:
    name: consul-provider-payment
  cloud:
    consul:
      host: 172.16.13.140
      port: 8500
      discovery:
        service-name: ${spring.application.name}